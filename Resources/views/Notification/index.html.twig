<h1>Message list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Message</th>
            <th>Isprivate</th>
            <th>Isblocker</th>
            <th>Issystem</th>
            <th>Updated</th>
            <th>Created</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('messages_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.message }}</td>
            <td>{{ entity.isPrivate }}</td>
            <td>{{ entity.isBlocker }}</td>
            <td>{{ entity.isSystem }}</td>
            <td>{% if entity.updated %}{{ entity.updated|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{% if entity.created %}{{ entity.created|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('messages_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('messages_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('messages_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
