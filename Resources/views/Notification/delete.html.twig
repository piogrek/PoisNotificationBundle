<p>{{ 'Zamierzasz usunąć powiadomienie '|trans({}, 'GrafixWebBundle') }} '{{ entity.notificationType.name }}' </p>
<strong>{{ 'header.are_you_sure'|trans({}, 'GrafixCrmBundle') }}</strong>
<form action="{{ path('notification_delete', { 'id': entity.id }) }}" method="post">
    {{ form_widget(delete_form) }}
</form>